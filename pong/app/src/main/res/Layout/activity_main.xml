<?xml version="1.0" encoding="utf-8"?>
<!-- 
    Main layout file - not actually used since we set GameView programmatically
    This is here for completeness but the activity uses setContentView(gameView) directly
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    
    <!-- GameView is added programmatically in MainActivity -->
    
</FrameLayout>